# C-Drive-Space-Monitor
This is a simple PowerShell script designed to monitor the storage space of the system's local disk (C:). It calculates the total space, used space, and free space in Gigabytes (GB).  The script also calculates the usage percentage and provides a warning message if the disk is more than 75% full.
